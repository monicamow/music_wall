<% if @vote && @vote.errors.any? %>
  <div style="color: #CC0000; display: inline-block;" class="alert alert-danger">
    <% @vote.errors.full_messages.each do |message| %>
      <%= message %>
    <% end %>
  </div>
<% end %>

<a href='/tracks'>Back to All Tracks</a>
